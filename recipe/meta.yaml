{% set name = "PyGithub" %}
{% set version = "1.43.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 5d3e3bdee80be63a1e5aaca5689ff450a4e7f7dfac728577fdb9caac080c5479

build:
  # Cannot use `noarch: python` as this uses 2to3.
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - pip
    
  run:
    - python
    - pyjwt

test:
  imports:
    - github
    - github.tests

about:
  home: http://pygithub.github.io/PyGithub/v1/index.html
  license_file: COPYING.LESSER
  license: LGPL-3.0
  license_family: LGPL
  summary: 'Python library implementing the GitHub API v3'
  doc_url: http://pygithub.github.io/PyGithub/v1/index.html
  dev_url: https://github.com/PyGithub/PyGithub

extra:
  recipe-maintainers:
    - jakirkham
    - pelson
    - xmnlab
